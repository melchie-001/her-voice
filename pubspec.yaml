name: your_app_name
description: A Flutter app with Firebase authentication and anonymous messaging.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.0
  firebase_auth: ^4.15.0
  firebase_database: ^10.5.0   # ✅ add this for Realtime Database
  http: ^1.1.0
  url_launcher: ^6.1.0

  # UI and utility packages
  cupertino_icons: ^1.0.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
